# Product Design QA

## 基准

- 视觉方向: `docs/product-design/selected-direction-media-lab.png`
- 实现截图: `docs/product-design/implementation-1440x1024.png`
- 对照证据: `docs/product-design/qa-side-by-side.png`
- 视口: `1440x1024`
- 状态: 默认示例资源，无上传视频，选中 `AI 输出`

## 检查范围

- 顶部栏、上传入口、导出入口
- 四段动作时间线和拖拽把手
- 抽帧设置、抠图设置、帧样本栅格
- 动作预览、Cloak 清单 JSON 预览
- 中文界面文案和高密度工作台布局

## 结果

通过。当前实现与选中的 media lab 方向一致：浅色工作台、左侧流程、中间素材与时间线、右侧检查器的结构已经落地，界面无明显遮挡、错位或空白渲染问题。

## 发现

- P0/P1/P2: 无需要阻塞交付的设计或实现问题。
- P3: 示例宠物使用 Cloak 项目里的 pixel-cat 资源，并非方向图中的灰色角色；这是数据占位差异，不影响工具流程。
- P3: 当前抠图为色键抠图，适合纯色背景视频；后续如果需要复杂背景，可接入浏览器端分割模型。
- P3: 方向图是英文参考，实现已按用户要求整体中文化，因此标题与标签存在预期差异。

## 已做修正

- 将界面里的 `Manifest` 标签替换为 `清单` / `清单 JSON`，减少中英混杂。

## 最终结论

passed
